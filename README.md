# Heart Disease ML System

This project is a machine learning system for predicting heart disease.

## Setup
1. Install dependencies: `pip install -r requirements.txt`
2. Run the application: `python main.py`
